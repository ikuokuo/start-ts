import { ZipCodeValidator } from "../1_export/ZipCodeValidator";
{
  let myValidator = new ZipCodeValidator();
}

import { ZipCodeValidator as ZCV } from "../1_export/ZipCodeValidator";
{
  let myValidator = new ZCV();
}

import * as validator from "../1_export/ZipCodeValidator";
{
  let myValidator = new validator.ZipCodeValidator();
}
